<h1>Listing orders</h1>

<table>
  <tr>
    <th>Customer</th>
    <th>Date ordered</th>
    <th>Total</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @orders.each do |orders| %>
  <tr>
    <td><%= orders.customer_id %></td>
    <td><%= orders.date_ordered %></td>
    <td><%= orders.total %></td>
    <td><%= link_to 'Show', orders %></td>
    <td><%= link_to 'Edit', edit_order_path(orders) %></td>
    <td><%= link_to 'Destroy', orders, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Customer order', new_order_path %>
